# Crashes

Crash inputs live here until they are confirmed as real issues, false positives,
or reproducible PoCs.

- `untriaged/`: found crashes with incomplete root cause analysis.
- `confirmed/`: real target crashes that still need PoC packaging or disclosure work.
- `false-positive/`: sanitizer/runtime/tooling artifacts that are not target bugs.
